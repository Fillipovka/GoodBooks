<%= form_for(current_user.likes.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :post_id, @post.id %></div>
  <%= button_tag(type: 'submit') do %>
		Like <span class="glyphicon glyphicon-heart">
	<% end %>
<% end %>
