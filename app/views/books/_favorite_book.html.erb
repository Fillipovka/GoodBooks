<%= form_for(current_user.favorite_books.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :book_id, @book.id %></div>
  <%= button_tag(type: 'submit') do %>
		<input type="image" src="avatar.jpg" />
	<% end %>
<% end %>
